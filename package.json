{
  "name": "units",
  "version": "0.0.1",
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.0",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "source-map-support": "^0.5.6",
    "typescript": "^2.8.3"
  },
  "dependencies": {
  },
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "scripts": {
    "compile": "rm -rf lib/ && tsc",
    "test": "npm run compile && mocha --require source-map-support/register out/tests",
    "prepublish": "npm run compile"
  }
}
